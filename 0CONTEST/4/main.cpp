#include <bits/stdc++.h>
using namespace std;

int main() {
    int t; cin>>t; while(t--) {
        int n,m; cin>>n>>m;
        vector<string> g(n);
        for (int i = 0; i < n; ++i) cin>>g[i];
        for (int i = 0; i < n; ++i) {
            for (int j = 0; j < i; ++j) {

            }
        }
    }
}
